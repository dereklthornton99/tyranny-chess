# Generated puzzle sets live here.
`puzzles.json` is written by `tools/gen-puzzles.js` (LANE-GEN).
The file currently committed is a hand-verified fixture so the page had something to load before the generator existed.
